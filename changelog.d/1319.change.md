The combination of a `__attrs_pre_init__` that takes arguments, a keyword-only field, and a default or factory on that field does not crash anymore, and the hook now receives the final selected value -- caller value, plain default, or factory result -- for both positional and keyword-only fields.
